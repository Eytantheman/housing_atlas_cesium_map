{
  "buildCommand": "cd server && npm install && cd ../client && npm install && npm run build && cp -r data ../api/data",
  "outputDirectory": "client/dist",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/index" },
    { "source": "/(.*)",     "destination": "/index.html" }
  ]
}
